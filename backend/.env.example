# Copy to .env and adjust as needed.
DATABASE_URL=sqlite:///./camarond.db
# Comma-separated list of allowed origins for CORS / Socket.IO.
# Use "*" for local dev + ngrok. Lock this down before any real deployment.
CORS_ORIGINS=*
# Password required to access the host console. Default: admin
HOST_PASSWORD=admin
